import os\nimport json\nfrom datetime import datetime, timedelta\nfrom typing import List, Dict, Tuple\nfrom collections import defaultdict, Counter\nimport statistics\n\n\nclass AnalyticsDashboard:\n    \"\"\"Advanced analytics and monitoring for RAG chatbot.\"\"\"\n    \n    def __init__(self, analytics_dir: str = \"./analytics\"):\n        self.analytics_dir = analytics_dir\n        os.makedirs(analytics_dir, exist_ok=True)\n        self.interactions_log = os.path.join(analytics_dir, \"interactions.jsonl\")\n        self.metrics_log = os.path.join(analytics_dir, \"metrics.jsonl\")\n    \n    def log_interaction(\n        self,\n        query: str,\n        answer: str,\n        sources: List[Dict],\n        quality_metrics: Dict,\n        response_time: float,\n        kb_name: str = \"default\"\n    ) -> None:\n        \"\"\"\n        Log user interaction.\n        \n        Args:\n            query: User query\n            answer: Generated answer\n            sources: Retrieved sources\n            quality_metrics: Quality evaluation metrics\n            response_time: Response time in seconds\n            kb_name: Knowledge base used\n        \"\"\"\n        interaction = {\n            'timestamp': datetime.now().isoformat(),\n            'query': query,\n            'query_length': len(query.split()),\n            'answer_length': len(answer.split()),\n            'sources_count': len(sources),\n            'quality_metrics': quality_metrics,\n            'response_time': response_time,\n            'kb_name': kb_name,\n            'quality_level': quality_metrics.get('quality_level', 'Unknown')\n        }\n        \n        with open(self.interactions_log, 'a', encoding='utf-8') as f:\n            f.write(json.dumps(interaction) + '\\n')\n    \n    def log_system_metrics(\n        self,\n        cpu_usage: float = None,\n        memory_usage: float = None,\n        db_size_mb: float = None,\n        chunk_count: int = None,\n        query_latency: float = None\n    ) -> None:\n        \"\"\"\n        Log system performance metrics.\n        \n        Args:\n            cpu_usage: CPU usage percentage\n            memory_usage: Memory usage percentage\n            db_size_mb: Database size in MB\n            chunk_count: Number of chunks in database\n            query_latency: Query latency in ms\n        \"\"\"\n        metrics = {\n            'timestamp': datetime.now().isoformat(),\n            'cpu_usage': cpu_usage,\n            'memory_usage': memory_usage,\n            'db_size_mb': db_size_mb,\n            'chunk_count': chunk_count,\n            'query_latency_ms': query_latency\n        }\n        \n        with open(self.metrics_log, 'a', encoding='utf-8') as f:\n            f.write(json.dumps(metrics) + '\\n')\n    \n    def get_interactions(\n        self,\n        hours: int = 24,\n        kb_name: str = None\n    ) -> List[Dict]:\n        \"\"\"\n        Get interactions from last N hours.\n        \n        Args:\n            hours: Number of hours to look back\n            kb_name: Filter by knowledge base name\n            \n        Returns:\n            List of interactions\n        \"\"\"\n        if not os.path.exists(self.interactions_log):\n            return []\n        \n        cutoff_time = datetime.now() - timedelta(hours=hours)\n        interactions = []\n        \n        with open(self.interactions_log, 'r', encoding='utf-8') as f:\n            for line in f:\n                if line.strip():\n                    interaction = json.loads(line)\n                    timestamp = datetime.fromisoformat(interaction['timestamp'])\n                    \n                    if timestamp < cutoff_time:\n                        continue\n                    \n                    if kb_name and interaction.get('kb_name') != kb_name:\n                        continue\n                    \n                    interactions.append(interaction)\n        \n        return interactions\n    \n    def get_interaction_statistics(self, hours: int = 24) -> Dict:\n        \"\"\"\n        Get statistics about user interactions.\n        \n        Args:\n            hours: Number of hours to analyze\n            \n        Returns:\n            Dictionary with statistics\n        \"\"\"\n        interactions = self.get_interactions(hours)\n        \n        if not interactions:\n            return {}\n        \n        response_times = [i['response_time'] for i in interactions]\n        quality_scores = [i['quality_metrics'].get('overall_score', 0) for i in interactions]\n        query_lengths = [i['query_length'] for i in interactions]\n        answer_lengths = [i['answer_length'] for i in interactions]\n        \n        stats = {\n            'total_interactions': len(interactions),\n            'time_period_hours': hours,\n            'average_response_time_s': round(statistics.mean(response_times), 2),\n            'median_response_time_s': round(statistics.median(response_times), 2),\n            'min_response_time_s': round(min(response_times), 2),\n            'max_response_time_s': round(max(response_times), 2),\n            'average_quality_score': round(statistics.mean(quality_scores), 1),\n            'median_quality_score': round(statistics.median(quality_scores), 1),\n            'quality_distribution': self._get_quality_distribution(interactions),\n            'average_query_length': round(statistics.mean(query_lengths), 0),\n            'average_answer_length': round(statistics.mean(answer_lengths), 0),\n            'average_sources_retrieved': round(statistics.mean([i['sources_count'] for i in interactions]), 1)\n        }\n        \n        return stats\n    \n    def _get_quality_distribution(self, interactions: List[Dict]) -> Dict:\n        \"\"\"\n        Get distribution of quality levels.\n        \n        Args:\n            interactions: List of interactions\n            \n        Returns:\n            Dictionary with quality distribution\n        \"\"\"\n        distribution = defaultdict(int)\n        \n        for interaction in interactions:\n            quality_level = interaction.get('quality_level', 'Unknown')\n            distribution[quality_level] += 1\n        \n        return dict(distribution)\n    \n    def get_top_queries(self, hours: int = 24, limit: int = 10) -> List[Dict]:\n        \"\"\"\n        Get most frequently asked queries.\n        \n        Args:\n            hours: Number of hours to analyze\n            limit: Number of top queries to return\n            \n        Returns:\n            List of top queries with counts\n        \"\"\"\n        interactions = self.get_interactions(hours)\n        query_counts = Counter(i['query'] for i in interactions)\n        \n        top_queries = []\n        for query, count in query_counts.most_common(limit):\n            top_queries.append({\n                'query': query,\n                'count': count,\n                'frequency': f\"{count / len(interactions) * 100:.1f}%\" if interactions else \"0%\"\n            })\n        \n        return top_queries\n    \n    def get_failed_queries(self, hours: int = 24, quality_threshold: float = 50) -> List[Dict]:\n        \"\"\"\n        Get queries with low quality answers.\n        \n        Args:\n            hours: Number of hours to analyze\n            quality_threshold: Quality score threshold\n            \n        Returns:\n            List of low-quality interactions\n        \"\"\"\n        interactions = self.get_interactions(hours)\n        failed = []\n        \n        for interaction in interactions:\n            score = interaction['quality_metrics'].get('overall_score', 100)\n            if score < quality_threshold:\n                failed.append({\n                    'query': interaction['query'],\n                    'quality_score': score,\n                    'quality_level': interaction.get('quality_level'),\n                    'timestamp': interaction['timestamp'],\n                    'response_time': interaction['response_time']\n                })\n        \n        # Sort by quality score\n        failed.sort(key=lambda x: x['quality_score'])\n        return failed[:10]  # Return bottom 10\n    \n    def get_kb_performance(self, hours: int = 24) -> Dict:\n        \"\"\"\n        Get performance metrics per knowledge base.\n        \n        Args:\n            hours: Number of hours to analyze\n            \n        Returns:\n            Dictionary with per-KB performance\n        \"\"\"\n        interactions = self.get_interactions(hours)\n        kb_stats = defaultdict(lambda: {'count': 0, 'scores': [], 'times': []})\n        \n        for interaction in interactions:\n            kb_name = interaction.get('kb_name', 'default')\n            kb_stats[kb_name]['count'] += 1\n            kb_stats[kb_name]['scores'].append(interaction['quality_metrics'].get('overall_score', 0))\n            kb_stats[kb_name]['times'].append(interaction['response_time'])\n        \n        result = {}\n        for kb_name, stats in kb_stats.items():\n            result[kb_name] = {\n                'interaction_count': stats['count'],\n                'average_quality_score': round(statistics.mean(stats['scores']), 1) if stats['scores'] else 0,\n                'average_response_time': round(statistics.mean(stats['times']), 2) if stats['times'] else 0,\n                'queries_per_hour': round(stats['count'] / hours, 2)\n            }\n        \n        return result\n    \n    def get_retrieval_analysis(self, hours: int = 24) -> Dict:\n        \"\"\"\n        Analyze document retrieval patterns.\n        \n        Args:\n            hours: Number of hours to analyze\n            \n        Returns:\n            Dictionary with retrieval analysis\n        \"\"\"\n        interactions = self.get_interactions(hours)\n        \n        if not interactions:\n            return {}\n        \n        sources_counts = [i['sources_count'] for i in interactions]\n        \n        analysis = {\n            'average_sources_retrieved': round(statistics.mean(sources_counts), 1),\n            'median_sources_retrieved': statistics.median(sources_counts),\n            'min_sources_retrieved': min(sources_counts),\n            'max_sources_retrieved': max(sources_counts),\n            'queries_with_zero_sources': sum(1 for c in sources_counts if c == 0),\n            'queries_with_multiple_sources': sum(1 for c in sources_counts if c > 1)\n        }\n        \n        return analysis\n    \n    def get_time_series_data(\n        self,\n        hours: int = 24,\n        interval_minutes: int = 60\n    ) -> Dict:\n        \"\"\"\n        Get time series data for visualization.\n        \n        Args:\n            hours: Number of hours to analyze\n            interval_minutes: Interval in minutes\n            \n        Returns:\n            Dictionary with time series data\n        \"\"\"\n        interactions = self.get_interactions(hours)\n        \n        # Initialize time buckets\n        now = datetime.now()\n        buckets = {}\n        for i in range(0, hours * 60, interval_minutes):\n            bucket_time = (now - timedelta(minutes=i)).isoformat()\n            buckets[bucket_time] = {'count': 0, 'avg_score': 0, 'scores': []}\n        \n        # Fill buckets\n        for interaction in interactions:\n            timestamp = datetime.fromisoformat(interaction['timestamp'])\n            bucket_idx = int((now - timestamp).total_seconds() / (interval_minutes * 60))\n            bucket_idx = min(bucket_idx, len(buckets) - 1)\n            \n            bucket_times = sorted(buckets.keys(), reverse=True)\n            if bucket_idx < len(bucket_times):\n                bucket_time = bucket_times[bucket_idx]\n                buckets[bucket_time]['count'] += 1\n                buckets[bucket_time]['scores'].append(\n                    interaction['quality_metrics'].get('overall_score', 0)\n                )\n        \n        # Calculate averages\n        time_series = {}\n        for bucket_time, data in sorted(buckets.items()):\n            if data['scores']:\n                data['avg_score'] = round(statistics.mean(data['scores']), 1)\n            else:\n                data['avg_score'] = 0\n            time_series[bucket_time] = {'count': data['count'], 'avg_quality': data['avg_score']}\n        \n        return time_series\n    \n    def generate_report(self, hours: int = 24) -> Dict:\n        \"\"\"\n        Generate comprehensive analytics report.\n        \n        Args:\n            hours: Number of hours to analyze\n            \n        Returns:\n            Dictionary with complete report\n        \"\"\"\n        report = {\n            'generated_at': datetime.now().isoformat(),\n            'time_period_hours': hours,\n            'interaction_statistics': self.get_interaction_statistics(hours),\n            'kb_performance': self.get_kb_performance(hours),\n            'retrieval_analysis': self.get_retrieval_analysis(hours),\n            'top_queries': self.get_top_queries(hours),\n            'failed_queries': self.get_failed_queries(hours),\n            'time_series': self.get_time_series_data(hours)\n        }\n        \n        return report\n    \n    def export_report(self, hours: int = 24, output_path: str = None) -> str:\n        \"\"\"\n        Export analytics report to file.\n        \n        Args:\n            hours: Number of hours to analyze\n            output_path: Path to save report\n            \n        Returns:\n            Path to saved report\n        \"\"\"\n        if not output_path:\n            timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n            output_path = os.path.join(self.analytics_dir, f\"report_{timestamp}.json\")\n        \n        report = self.generate_report(hours)\n        \n        with open(output_path, 'w', encoding='utf-8') as f:\n            json.dump(report, f, indent=2)\n        \n        print(f\"✓ Report exported to: {output_path}\")\n        return output_path\n    \n    def get_summary(self, hours: int = 24) -> str:\n        \"\"\"\n        Get human-readable summary of analytics.\n        \n        Args:\n            hours: Number of hours to analyze\n            \n        Returns:\n            Summary string\n        \"\"\"\n        stats = self.get_interaction_statistics(hours)\n        kb_perf = self.get_kb_performance(hours)\n        retrieval = self.get_retrieval_analysis(hours)\n        \n        summary = f\"\"\"\n╔════════════════════════════════════════════════════════════╗\n║           RAG CHATBOT ANALYTICS SUMMARY                    ║\n╚════════════════════════════════════════════════════════════╝\n\nAnalysis Period: Last {hours} hours\n\n📊 Interaction Statistics:\n  • Total Interactions: {stats.get('total_interactions', 0)}\n  • Avg Response Time: {stats.get('average_response_time_s', 0)}s\n  • Avg Quality Score: {stats.get('average_quality_score', 0)}/100\n  • Quality Distribution: {stats.get('quality_distribution', {})}\n\n🔍 Retrieval Analysis:\n  • Avg Sources Retrieved: {retrieval.get('average_sources_retrieved', 0)}\n  • Queries with 0 Sources: {retrieval.get('queries_with_zero_sources', 0)}\n  • Queries with Multiple Sources: {retrieval.get('queries_with_multiple_sources', 0)}\n\n📈 Knowledge Base Performance:\n\"\"\"\n        for kb_name, perf in kb_perf.items():\n            summary += f\"\"\"\n  {kb_name}:\n    - Interactions: {perf['interaction_count']}\n    - Avg Quality: {perf['average_quality_score']}/100\n    - Avg Response Time: {perf['average_response_time']}s\n    - Queries/Hour: {perf['queries_per_hour']}\n\"\"\"\n        \n        summary += \"\\n\" + \"=\"*60 + \"\\n\"\n        \n        return summary\n\n\nif __name__ == \"__main__\":\n    dashboard = AnalyticsDashboard()\n    \n    print(\"Analytics Dashboard Initialized\")\n    print(f\"Interactions Log: {dashboard.interactions_log}\")\n    print(f\"Metrics Log: {dashboard.metrics_log}\")"
