import json\nfrom datetime import datetime\nfrom typing import List, Dict, Tuple\nfrom enum import Enum\nimport re\n\n\nclass QualityMetric(Enum):\n    \"\"\"Enum for different quality metrics.\"\"\"\n    RELEVANCE = \"relevance\"\n    COMPLETENESS = \"completeness\"\n    ACCURACY = \"accuracy\"\n    CLARITY = \"clarity\"\n    COHERENCE = \"coherence\"\n    HALLUCINATION_RISK = \"hallucination_risk\"\n\n\nclass AnswerQualityEvaluator:\n    \"\"\"Evaluates quality of generated answers based on multiple metrics.\"\"\"\n    \n    def __init__(self):\n        self.evaluation_history = []\n    \n    def evaluate_relevance(self, answer: str, query: str, context: str) -> float:\n        \"\"\"\n        Evaluate how relevant the answer is to the query.\n        \n        Args:\n            answer: Generated answer\n            query: User query\n            context: Retrieved context\n            \n        Returns:\n            Relevance score (0-1)\n        \"\"\"\n        query_words = set(query.lower().split())\n        answer_words = set(answer.lower().split())\n        \n        # Calculate word overlap\n        overlap = len(query_words.intersection(answer_words))\n        max_words = max(len(query_words), len(answer_words))\n        \n        relevance_score = min(1.0, overlap / max(max_words, 1))\n        \n        # Check if answer addresses the query\n        if any(q_word in answer.lower() for q_word in query_words if len(q_word) > 3):\n            relevance_score = min(1.0, relevance_score + 0.2)\n        \n        return min(1.0, relevance_score)\n    \n    def evaluate_completeness(self, answer: str, context: str) -> float:\n        \"\"\"\n        Evaluate if the answer is complete and addresses all aspects.\n        \n        Args:\n            answer: Generated answer\n            context: Retrieved context\n            \n        Returns:\n            Completeness score (0-1)\n        \"\"\"\n        # Check answer length\n        answer_length = len(answer.split())\n        context_length = len(context.split())\n        \n        # Reasonable answer should be 20-30% of context\n        ideal_length = context_length * 0.25\n        \n        if answer_length < 10:\n            return 0.3  # Too short\n        elif answer_length < 50:\n            return 0.6\n        elif answer_length < ideal_length * 1.5:\n            return 0.9\n        else:\n            return 0.7  # Possibly too long\n    \n    def evaluate_clarity(self, answer: str) -> float:\n        \"\"\"\n        Evaluate clarity of the answer.\n        \n        Args:\n            answer: Generated answer\n            \n        Returns:\n            Clarity score (0-1)\n        \"\"\"\n        sentences = answer.split('.')\n        if len(sentences) < 2:\n            return 0.4  # Too few sentences\n        \n        avg_sentence_length = len(answer.split()) / len(sentences)\n        \n        # Ideal sentence length is 10-20 words\n        if avg_sentence_length < 5:\n            return 0.5  # Too fragmented\n        elif avg_sentence_length > 30:\n            return 0.6  # Too complex\n        else:\n            return 0.9  # Good clarity\n    \n    def evaluate_coherence(self, answer: str) -> float:\n        \"\"\"\n        Evaluate coherence and logical flow of answer.\n        \n        Args:\n            answer: Generated answer\n            \n        Returns:\n            Coherence score (0-1)\n        \"\"\"\n        # Check for transition words\n        transition_words = [\n            'furthermore', 'therefore', 'however', 'moreover',\n            'additionally', 'thus', 'hence', 'consequently',\n            'as a result', 'in addition', 'similarly', 'in contrast'\n        ]\n        \n        answer_lower = answer.lower()\n        transition_count = sum(1 for word in transition_words if word in answer_lower)\n        \n        # Sentences should build on each other\n        sentences = [s.strip() for s in answer.split('.') if s.strip()]\n        \n        coherence_score = min(1.0, 0.5 + (transition_count / max(len(sentences), 1)) * 0.5)\n        \n        return coherence_score\n    \n    def evaluate_hallucination_risk(self, answer: str, context: str) -> float:\n        \"\"\"\n        Evaluate risk of hallucination (claims not in context).\n        \n        Args:\n            answer: Generated answer\n            context: Retrieved context\n            \n        Returns:\n            Hallucination risk score (0-1, where 0=no risk, 1=high risk)\n        \"\"\"\n        # Extract key claims from answer\n        sentences = [s.strip() for s in answer.split('.') if s.strip()]\n        \n        context_lower = context.lower()\n        unverified_claims = 0\n        \n        for sentence in sentences:\n            # Skip generic sentences\n            if len(sentence.split()) < 4:\n                continue\n            \n            # Check if key parts of sentence are in context\n            sentence_words = set(sentence.lower().split())\n            context_words = set(context_lower.split())\n            \n            overlap = len(sentence_words.intersection(context_words))\n            coverage = overlap / max(len(sentence_words), 1)\n            \n            if coverage < 0.3:  # Less than 30% of sentence words in context\n                unverified_claims += 1\n        \n        hallucination_risk = min(1.0, unverified_claims / max(len(sentences), 1))\n        \n        return hallucination_risk\n    \n    def evaluate_source_citation(self, answer: str, sources: List[Dict]) -> float:\n        \"\"\"\n        Evaluate if sources are properly cited.\n        \n        Args:\n            answer: Generated answer\n            sources: List of sources used\n            \n        Returns:\n            Citation score (0-1)\n        \"\"\"\n        if not sources:\n            return 0.0  # No sources provided\n        \n        # Check if answer mentions sources\n        source_mentions = 0\n        for source in sources:\n            source_title = source.get('title', '').lower()\n            if source_title in answer.lower():\n                source_mentions += 1\n        \n        citation_score = min(1.0, source_mentions / max(len(sources), 1))\n        \n        return citation_score\n    \n    def evaluate_accuracy(self, answer: str, context: str) -> float:\n        \"\"\"\n        Evaluate factual accuracy based on context.\n        \n        Args:\n            answer: Generated answer\n            context: Retrieved context\n            \n        Returns:\n            Accuracy score (0-1)\n        \"\"\"\n        # This is a simplified metric - in production, use fact-checking APIs\n        # Check for specific patterns that might indicate inaccuracy\n        \n        inaccuracy_indicators = [\n            r'\\b(?:i think|i believe|probably|maybe|might|could be)\\b',  # Uncertainty\n            r'\\b(?:no data|unknown|unclear|not sure)\\b'  # Lack of info\n        ]\n        \n        uncertainty_count = 0\n        for pattern in inaccuracy_indicators:\n            matches = re.findall(pattern, answer.lower())\n            uncertainty_count += len(matches)\n        \n        # Penalize for uncertainty expressions\n        accuracy_score = max(0.0, 1.0 - (uncertainty_count * 0.1))\n        \n        # Boost if claims are well-supported in context\n        context_coverage = self._calculate_context_coverage(answer, context)\n        accuracy_score = (accuracy_score * 0.6) + (context_coverage * 0.4)\n        \n        return min(1.0, accuracy_score)\n    \n    def _calculate_context_coverage(self, answer: str, context: str) -> float:\n        \"\"\"\n        Calculate what percentage of answer is covered by context.\n        \n        Args:\n            answer: Generated answer\n            context: Retrieved context\n            \n        Returns:\n            Coverage percentage (0-1)\n        \"\"\"\n        answer_words = set(answer.lower().split())\n        context_words = set(context.lower().split())\n        \n        # Remove common stop words\n        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been',\n                     'and', 'or', 'but', 'in', 'on', 'at', 'to', 'for', 'of', 'with'}\n        \n        answer_words = answer_words - stop_words\n        context_words = context_words - stop_words\n        \n        if not answer_words:\n            return 0.0\n        \n        coverage = len(answer_words.intersection(context_words)) / len(answer_words)\n        return coverage\n    \n    def get_overall_score(self, evaluation: Dict) -> float:\n        \"\"\"\n        Calculate overall quality score from individual metrics.\n        \n        Args:\n            evaluation: Dictionary of evaluation metrics\n            \n        Returns:\n            Overall quality score (0-100)\n        \"\"\"\n        weights = {\n            'relevance': 0.2,\n            'completeness': 0.2,\n            'accuracy': 0.25,\n            'clarity': 0.15,\n            'coherence': 0.1,\n            'hallucination_risk': -0.1  # Negative weight (lower is better)\n        }\n        \n        overall_score = 0\n        for metric, weight in weights.items():\n            if metric in evaluation:\n                if metric == 'hallucination_risk':\n                    # Invert hallucination risk (1 - risk)\n                    overall_score += (1 - evaluation[metric]) * weight * 100\n                else:\n                    overall_score += evaluation[metric] * weight * 100\n        \n        return min(100, max(0, overall_score))\n    \n    def evaluate_answer(\n        self,\n        answer: str,\n        query: str,\n        context: str,\n        sources: List[Dict]\n    ) -> Dict:\n        \"\"\"\n        Comprehensive evaluation of an answer.\n        \n        Args:\n            answer: Generated answer\n            query: User query\n            context: Retrieved context\n            sources: List of sources\n            \n        Returns:\n            Dictionary with all evaluation metrics\n        \"\"\"\n        evaluation = {\n            'timestamp': datetime.now().isoformat(),\n            'answer_length': len(answer.split()),\n            'metrics': {\n                'relevance': self.evaluate_relevance(answer, query, context),\n                'completeness': self.evaluate_completeness(answer, context),\n                'accuracy': self.evaluate_accuracy(answer, context),\n                'clarity': self.evaluate_clarity(answer),\n                'coherence': self.evaluate_coherence(answer),\n                'hallucination_risk': self.evaluate_hallucination_risk(answer, context),\n                'citation_score': self.evaluate_source_citation(answer, sources)\n            }\n        }\n        \n        # Calculate overall score\n        evaluation['overall_score'] = self.get_overall_score(evaluation['metrics'])\n        \n        # Determine quality level\n        score = evaluation['overall_score']\n        if score >= 85:\n            evaluation['quality_level'] = 'Excellent'\n        elif score >= 70:\n            evaluation['quality_level'] = 'Good'\n        elif score >= 50:\n            evaluation['quality_level'] = 'Fair'\n        else:\n            evaluation['quality_level'] = 'Poor'\n        \n        # Store in history\n        self.evaluation_history.append(evaluation)\n        \n        return evaluation\n    \n    def get_statistics(self) -> Dict:\n        \"\"\"\n        Get statistics from evaluation history.\n        \n        Returns:\n            Dictionary with statistics\n        \"\"\"\n        if not self.evaluation_history:\n            return {}\n        \n        scores = [e['overall_score'] for e in self.evaluation_history]\n        \n        stats = {\n            'total_evaluations': len(self.evaluation_history),\n            'average_score': sum(scores) / len(scores),\n            'min_score': min(scores),\n            'max_score': max(scores),\n            'quality_distribution': self._get_quality_distribution()\n        }\n        \n        return stats\n    \n    def _get_quality_distribution(self) -> Dict:\n        \"\"\"\n        Get distribution of quality levels.\n        \n        Returns:\n            Dictionary with quality level counts\n        \"\"\"\n        distribution = {'Excellent': 0, 'Good': 0, 'Fair': 0, 'Poor': 0}\n        \n        for evaluation in self.evaluation_history:\n            level = evaluation.get('quality_level', 'Unknown')\n            if level in distribution:\n                distribution[level] += 1\n        \n        return distribution\n\n\nif __name__ == \"__main__\":\n    evaluator = AnswerQualityEvaluator()\n    \n    # Test evaluation\n    test_answer = \"Machine Learning is a subset of artificial intelligence that enables systems to learn from data without being explicitly programmed. It uses algorithms to identify patterns and make predictions.\"\n    test_query = \"What is Machine Learning?\"\n    test_context = \"Machine Learning is a field of artificial intelligence that focuses on enabling computers to learn from data. ML algorithms can identify patterns, make predictions, and improve performance through experience without being explicitly programmed for each specific task.\"\n    test_sources = [{'title': 'Machine Learning Basics', 'url': 'https://example.com', 'max_similarity': 0.85}]\n    \n    evaluation = evaluator.evaluate_answer(test_answer, test_query, test_context, test_sources)\n    \n    print(\"\\nAnswer Quality Evaluation:\")\n    print(f\"Overall Score: {evaluation['overall_score']:.1f}/100\")\n    print(f\"Quality Level: {evaluation['quality_level']}\")\n    print(\"\\nMetrics:\")\n    for metric, score in evaluation['metrics'].items():\n        print(f\"  {metric}: {score:.2f}\")"
